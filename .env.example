PORT=4000
JWT_SECRET=change-this-to-a-long-random-string-in-production
DB_FILE=./terex.db

# Optional — powers "Deteksi dari BKB" on Goods Receipt (POST /api/stock/parse-bkb).
# Get a key at https://console.anthropic.com. Leaving this unset just disables
# that one feature (503) — everything else works the same without it.
# ANTHROPIC_API_KEY=
# ANTHROPIC_MODEL=claude-sonnet-5
